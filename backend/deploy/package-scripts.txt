# Adicionar estas linhas ao package.json na seção scripts
"scripts": {
  # ... scripts existentes ...
  "docker:build": "docker build -f deploy/Dockerfile -t lushamm-backend .",
  "docker:dev": "docker-compose -f deploy/docker-compose.yml up -d",
  "docker:prod": "NODE_ENV=production docker-compose -f deploy/docker-compose.yml up -d",
  "docker:stop": "docker-compose -f deploy/docker-compose.yml down",
  "docker:logs": "docker-compose -f deploy/docker-compose.yml logs -f",
  "deploy:oracle": "cd deploy && ./deploy.sh"
}
